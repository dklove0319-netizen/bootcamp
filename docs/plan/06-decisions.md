# 결정 기록 — 오제로의 거울

> 방향을 정하거나 바꾼 결정을 위에서부터 쌓는다(최신이 위). 한 결정 = 4줄.

## [2026-07-05] 무료 측정에도 "사용자 먼저 구별" 적용 + 익명 아이디 저장 방향 (사용자 지시)
- 왜: 사용자 지시 — 기록 뒤에 사용자가 스스로 사실·망상을 나누는 화면 추가(제공된 판별 키워드 데이터셋 포함), 기록은 저장돼야 하며 익명 오제로 아이디 발급 + 배달 시간 설정으로 해결. AI 먼저 답 주던 맛보기 예외가 사라져 지시서 순서 원칙(5→6)과 완전 일치
- 버린 대안: AI 즉시 판독(기존 — 의존 유도 위험) / 이메일 가입 후 저장(문턱 높음)
- 영향: 측정 흐름 4화면(기록→구별→대조), 홈 태그라인 제거, 판별 키워드 사전을 프롬프트·08-ai-prompts에 반영, 저장+익명 아이디+배달 시간은 블럭 3에서 구현(S17 규칙 확정), 결과 화면에 사용자 구별 회수 표시

## [2026-07-05] AI 모델 분리 확정 — 무료 측정 = Sonnet, 21일 코스 = Opus 4.8 (사용자 확정)
- 왜: 사용자 선택 — 첫인상(무료 측정)의 판독 품질을 우선. 비용: 측정 1회 Sonnet ≈ 13원(인트로가, 2026-09부터 ≈19원) vs Haiku ≈ 6원 — 2~3배지만 절대액 작음. 코스는 Opus 4.8(품질 최우선, 1인 21일 ≈ 3천~5천 원 = 가격의 6~9%)
- 버린 대안: 측정 Haiku(최저가 — 첫인상 품질 양보) / 전부 Opus(측정 1회 ≈ 32원, 무료층 과다)
- 영향: apps/web/lib/ai-models.ts 에서 환경변수로 교체 가능(MEASURE_MODEL/COURSE_MODEL) — 비용이 커지면 코드 수정 없이 전환. next.config.mjs 가 루트 .env 를 서버로 로드하도록 수정

## [2026-07-05] 무료층 = 2단 (비로그인 맛보기 + 가입 후 무료 3일 미니 체험) — 스펙 "단발 측정" 대체
- 왜: 사용자 목표가 질 좋은 데이터인데 단발 측정 1건은 패턴 관찰 불가(반복은 2회↑ 필요)이고 비로그인은 저장도 안 해 데이터 0. 3일 체험이라야 반복의 시작·재방문 행동이 쌓임. journeys.course='trial3'로 기존 구조 재사용
- 버린 대안: 단발 측정만(데이터 빈약 — 사용자가 지적) / 가입 우선 3일만(문턱 0 입구 상실) / 3일 유료 상품(스펙이 배제한 사다리 흐리기)
- 영향: 스키마에 measure_limits(익명 IP 제한)·consents.revoked_at 추가, journeys에 trial3, daily_entries 공용화. PRICING_SPEC 무료 정의 개정. 미니 거울은 진단 아닌 거울(명사화 금지) — 새 AI 동작·화면은 블럭 2에서. block1.sql 표 7개로 확정

## [2026-07-05] 동의 철회(revoked_at) + 익명 제한(measure_limits) 스키마 반영 (사용자 확정)
- 왜: PRICING_SPEC "동의 언제든 철회 + 연구 추출은 철회 안 함·환불 아님"을 담을 칸이 없었음(consents에 철회 시각 부재). 비로그인 1일 1회는 localStorage·클라 게이팅 금지라 서버가 IP로 판정해야 함
- 버린 대안: 철회 기능 빼기(스펙·개인정보 원칙 위반) / 무제한 허용(AI 비용·스펙 위반) / 비로그인 결과 저장(스펙 위반)
- 영향: consents.consented_at→granted_at + revoked_at, measure_limits 표(ip_hash·day·count, 기록 원문 미저장, RLS 서버 전용). 개인정보처리방침에 IP 사용 명시 필요(재검토 목록)

## [2026-07-05] 용어는 전 화면 "망상"으로 통일 — 이층 가정 폐기 (사용자 확정)
- 왜: 사용자 직접 지시 — 홈 문구 "사실과 해석" 을 보고 "망상으로 통일해줘". 판독 규칙의 용어 규칙("해석으로 순화 금지")이 무료 측정·공유 카드·랜딩까지 예외 없이 적용됨
- 버린 대안: 이층 사용(밖 "해석"/안 "망상" — 대중 마찰 줄이려던 작업 가정) — 브랜드 용어의 일관성이 우선
- 영향: ko.json 문구, PRICING_SPEC 측정값 표기("사실 n : 망상 m"), plan.md 2-2·2-3·6-1, S14·S17 명세. 공유 카드에 "망상"이 그대로 노출됨을 인지한 결정

## [2026-07-05] 21일 코스 가격 34,000 → 49,000 개정 (사용자 확정)
- 왜: 무료 측정(0층)이 바이럴·도달을 담당하므로 유료 가격의 유일한 임무는 표본 질 필터 — 이 세그먼트에 3만원대는 "가벼움" 신호. 또 120,000 취소선 대비 72% 할인은 정가 신뢰를 깎지만 59%(49,000)는 "검증 참여 조건부 할인" 명분과 맞음. 59,000이 아닌 이유: 검증 국면엔 표본 수도 필요
- 버린 대안: 34,000 유지(필터 약함·할인율 과다) / 59,000(초기 표본 수 위축)
- 영향: PRICING_SPEC 개정(49,000, 인상 로드맵 49→79→120 — 인상 근거는 후기·완주율·전환율), Q15·plan.md 블럭 10 갱신

## [2026-07-05] 21일 거울 보고서는 코스 가격에 포함, "패턴 지도"는 출시 후 별도 상품 (확정)
- 왜: 사용자 우선순위 = 질 좋은 데이터 > 바이럴. 보고서는 완주의 회수물 — 완주 동기가 곧 완주율이고 완주율이 곧 데이터 질. 끝에서 재결제를 걸면 완주 동기가 꺾이고(데이터 질 하락) 신뢰가 깨짐(클리프행어 과금 — 이미 버린 모델)
- 버린 대안: 보고서 별도 판매(완주율·신뢰 훼손) / 패턴 지도 가격 선결정(분류 체계의 원재료인 데이터가 아직 없어 지금 정하면 틀림)
- 영향: 결제 화면 문구 "21일 훈련 + 거울 보고서 포함", 패턴 지도는 PRD 로드맵으로(첫 완주 기수 데이터로 운영자 수작업 프로토타입 → 가격 발견), 지시서의 "패턴 분류 = 운영자 오프라인 자산" 원칙과 정합

## [2026-07-05] PRICING_SPEC.md 채택 — 무료 측정 퍼널 + 34,000원 검증 코스 (오전의 "베타 무료" 전략 대체)
- 왜: 사용자 확정 스펙 — 무료 즉시 측정(비로그인 1일 1회·로그인 3회)이 바이럴 입구, 21일 코스 34,000원(정가 120,000 취소선 · 연구 동의가 할인 조건 · 34,000→89,000→120,000 인상 로드맵), 환불 72시간+기록 3건 이하, 가격 하드코딩 금지
- 버린 대안: 수료자 수동 등록 무료 베타(입구가 좁아 데이터 수집 느림) / 3일 체험 상품(스펙이 명시 배제 — 환불 규칙이 대체)
- 영향: plan.md 빌드 순서 재편(무료 측정이 블럭 2), consents 에 revoked_at 추가(ALTER — 블럭 3), 공유 카드 블럭 신설, CLAUDE.md 에 스펙 필독 연결, 브랜드 색 #fff6e9/#545454 적용

## [2026-07-05] 용어 이층 사용 — 밖은 "해석", 안은 "망상" (작업 가정 · 사용자 확인 대기)
- 왜: 판독 규칙은 "망상 순화 금지", PRICING_SPEC 은 "사실/해석 측정값"·"사실 2 : 해석 7" — 두 지시가 충돌. 무료 측정·공유 카드·랜딩(대중 첫 접촉)은 "해석", 21일 코스 내부(약속된 훈련 용어)는 "망상"으로 층을 나눈다
- 버린 대안: 전부 "망상"(첫 방문자에게 낯섦·오해 위험) / 전부 "해석"(훈련 용어의 의도 상실)
- 영향: 무료 측정·공유 카드 문구는 "해석", 코스 안은 "망상". 문구가 전부 ko.json 에 있어 언제든 일괄 변경 가능

## [2026-07-05] 여정(journeys) 구조 채택 — 35일 심화와 "새 21일"의 자리를 창고에 지금 만든다
- 왜: 사용자 정보 — 35일 심화(주차별 주제·메스 모드)는 구조가 같고 코스만 다름. 스키마는 나중에 못 바꾼다고 간주하므로 코스 개념(journeys.course)을 표 생성 전에 넣어야 함. "새 21일 시작"(Q13)도 이 구조라야 표현됨
- 버린 대안: 35일용 앱 별도 제작(중복 개발·데이터 분산) / 21일 하드코딩 후 개조(창고 뜯는 공사)
- 영향: journeys 표 신설, daily_entries.journey_id, profiles.start_date는 journeys로 이동, day_no는 여정 기준. **v1 빌드 범위는 여전히 21일 거울 코스만** (지시서 11번 — 한 번에 전체 스펙 금지)

## [2026-07-05] 가격 전략 — 정가 선(先)명시 + 베타 무료, 목표 정가 10만원대 (액수는 데이터 후 확정)
- 왜: 사용자 방향 — 지금은 검증 단계(바이럴·데이터 수집), 추후 10만원대 목표. 정가 없이 무료로 시작하면 "무료 앱"으로 각인돼 인상 시 이탈·반발 — 정가를 먼저 걸고 "베타 기수 무료"로 표기하면 전환이 할인 종료가 됨
- 버린 대안: 무료로 조용히 시작 후 인상(각인 문제) / 대화량 과금(사주 앱 모델 — 의존을 파는 구조라 절대 원칙 1·오제로 철학과 정면충돌)
- 영향: 결제·랜딩 화면 문구(정가 + 베타 무료 병기), 측정 설계(완주율·전후 변화)가 정가의 근거 자산이 됨, Q15는 액수만 남음

## [2026-07-05] 익명 코드는 레터 체계를 이어받는다 — o056부터 자동 발급, 기존 코드는 수동 지정
- 왜: 사용자 정보 — 오제로 레터에서 이미 o055까지 발급됨. 수료자의 코드 정체성이 앱에서도 이어져야 함
- 버린 대안: 앱에서 o001부터 새로 시작 — 레터 수료자가 코드를 두 개 갖게 되고 "가입 순번 기반"의 의미가 깨짐
- 영향: DB에 번호표 기계(시퀀스, 시작값 56), 관리자 등록 화면에 코드 칸(선택), 형식 'o'+3자리 통일

## [2026-07-05] 자유 기록(3단계)에 쓰기 가이드 고정 — 검열 문턱 내리기
- 왜: 사용자 지시 — 무의식이 드러나려면 "잘 써야 한다"는 검열부터 꺼야 한다. 가이드는 내용 유도(주제·예시) 없이 형식의 허가만: 잘 쓸 필요 없음 · 아무렇게나 · 욕 허용 · 배설하듯 · 맞고 틀림 없음
- 버린 대안: 안내 최소주의("오늘 하루를 적어주세요"만) — 기록의 질이 사용자의 글쓰기 부담에 좌우됨 / 매일 다른 문구 — 기록 조건이 달라져 비교 오염
- 영향: S04 명세·02-spec·와이어프레임에 확정 문안 4줄 (21일 동일), 마지막 줄은 격리 층 사실만 약속 ("다른 관찰자에게 보이지 않아요")

## [2026-07-05] 위기 대응 = B안 "믿음 + 비상구" — 지시서 4단계(루프 중단·화면 전환)를 변경
- 왜: 사용자 결정 — 관점 원칙(환자·내담자로 안 봄)과 안전의 양립: 루프 무중단 + 하단 조용한 한 줄(109 · 닫기 가능) + 설정 상시 연락처. 감지는 키워드 1차 + AI 2차 유지, 위기 내용의 판독도 다른 기록과 동일하게
- 버린 대안: 지시서 원안 A(중단·전환 — 환자 취급 느낌 최대) / 완전 제거 C(AI 처리 규칙 부재로 톤 붕괴 위험·공개 확장 시 무방비)
- 영향: S05가 독립 화면에서 하단 구성 요소로 축소, S04·유저플로우·블럭 4·프롬프트 5번 갱신, 관리자 별도 통지 없음(대시보드 표시만), **공개 확장 전 재검토 항목으로 등록**

## [2026-07-05] 상태 체크 = "오늘의 눈금" 4종 + 시작점·21일째 검증 척도 2회 (Q2)
- 왜: 사용자 승인 — 캐묻기가 아니라 관찰·회수 구조(관찰형 라벨·기본값 없음·문구 21일 고정). 감정은 이름(균형 8개)+세기 — 명명 자체가 자기 인식 행위이자 7단계 잇기의 재료. 효과 입증은 WHO-5(웰빙)+탈중심화 척도(관찰자 능력 — 능력 진술형)로 전후 비교
- 버린 대안: 세기만(잇기 흐림) / CFQ-7(결핍 진술형 — 관점 원칙 충돌로 교체) / 설문 없음(입증력 약함) / 수면 시간 추가(매일 부담)
- 영향: daily_entries에 emotion_label 추가, assessments 표 신설, S03 전면 개정·S16 신설, 보고서에 전후 나란히, 동의 (c)에 연구 활용 명시, 척도 한국어판 사용 조건 확인 필요

## [2026-07-05] 시간·기록 정책 확정 (Q3·Q4·Q5)
- 왜: 사용자 승인 — 기록 창은 고정 시각~다음날 고정 시각(밤 사용자에게 자연), 1일차 = 첫 기록 제출일, 제출 후 수정 완전 불가(기록의 원본성 — "그날의 나"가 그대로 남는다), 시간대 v1 한국 고정
- 버린 대안: 자정 마감(늦은 시각 시작자에게 창이 3시간뿐) / 가입일 1일차(안 쓴 날이 소모됨) / 오타 5분 허용(오타와 내용 수정을 시스템이 구별 못함)
- 영향: 서버 게이팅·entry_date 판정 규칙, profiles.start_date는 첫 제출 시 기록, S10 문구

## [2026-07-05] 연동·운영 일괄 확정 (Q8~Q14·Q16)
- 왜: 사용자 승인 — 로그인 이메일 매직링크 / 관리자 최소안(등록·열람·지표 3종) / 푸시 웹 푸시 / 시각 변경 다음날 적용 / 익명 답변 같은 날짜 무작위 / 21일 후 열람+새 21일 / 보고서 7구성 / AI 프롬프트 Next.js 서버 라우트
- 버린 대안: 카카오 로그인·알림톡(실명 부담·건당 비용·심사) / Edge Functions(CLI 필요 — 킷 규칙과 마찰, 비노출 취지는 서버 라우트로 동일 충족)
- 영향: .env.example에 웹 푸시(VAPID) 키 추가, 아이폰 PWA 설치 안내 필요, 07-architecture 문 확정

## [2026-07-05] 구별 UI 확정 (Q1) — 기계적 조각내기 + 사용자 쪼개기 + 조각 태깅, 정밀 분해는 AI 대조가
- 왜: 사용자 지시 "혼합 문장에서 가장 정확하게 구별되게" — 정확도는 6단계 AI의 성분 분해(src 원문 검증)가 맡고, 5단계 화면은 판단 없는 기계적 분리(문장·연결어미 경계)만 해야 AI가 사용자보다 먼저 답을 주지 않는다
- 버린 대안: AI가 성분을 미리 나눠 제시 — 나눈 위치 자체가 힌트라 순서 원칙(5→6) 위반 / 문장 통째 태깅 — 혼합 문장에서 훈련이 뭉개짐
- 영향: S06 명세·02-spec 갱신, user_split 구조 확정([{src, label}]), plan.md 2-1 선결 해제

## [2026-07-05] 사실·망상 판별 기준 확정 — 카메라 검사 + 근거 검사, 성분 단위, 방향 중립
- 왜: 사용자(운영자) 직접 지시 — 모든 AI 동작의 공통 판독 규칙 (감정의 발생은 사실·해석은 망상, "망상" 용어 유지, 내부 범주 노출 금지, 판별 불가는 되묻기)
- 버린 대안: AI 재량의 문장 단위 분류 — 혼합 문장에서 사실과 망상이 뭉개짐
- 영향: CLAUDE.md 판독 규칙 · 08-ai-prompts.md 공통 판독 규칙 + 1번 프롬프트 전면 개정(성분 단위 출력 src/text/label/category, unclear 되묻기) · S06/S07 명세 문구

## [2026-07-05] 관점 원칙 채택 — 사용자는 환자·내담자가 아니라 관찰자다
- 왜: 사용자(운영자) 직접 지시 — 오제로는 사용자를 문제 있는·고쳐야 하는·아픈 사람으로 보지 않는다
- 버린 대안: 임상 도구·결핍 어휘를 그대로 쓰는 통상적 심리 앱 문법 — 브랜드 정체성과 충돌
- 영향: 전 화면 문구에서 결핍·치료 어휘 금지, 설문 도구는 능력 진술형 우선 (CLAUDE.md 관점 원칙 · PRD 3절 7항)

## [2026-07-05] AI 인용 문장은 표시 전에 원문과 문자열 일치 검증을 거친다
- 왜: 절대 원칙 2(지어내지 않는다)를 말이 아니라 코드로 강제 — AI가 원문을 한 글자라도 바꾸면 인용 자체를 버린다
- 버린 대안: AI 출력을 그대로 신뢰 — 의역·창작 인용이 섞일 위험
- 영향: 질문 생성·오프닝·보고서의 서버 로직에 검증 단계 추가, 인용 실패 시 "인용 없음" 경로 필요

## [2026-07-05] RLS는 표 생성 때부터 켜고, 블럭 1~4는 서버 경유 + 테스트 계정 1개로 개발한다
- 왜: 지시서 7번 "RLS를 처음부터" — 단 지시서 10번 빌드 순서상 로그인(인증)은 2단계라, 그 전엔 화면이 창고를 직접 읽지 않고 서버(주방)를 거쳐 고정 테스트 계정으로 읽고 쓴다
- 버린 대안: 킷 기본(개발 중 RLS 꺼두기) — 지시서가 우선. / 처음부터 인증 구현 — 첫 화면까지 너무 멀어짐
- 영향: 블럭 1~4의 데이터 접근은 전부 서버 라우트로, 블럭 5에서 실로그인으로 교체

## [2026-07-05] 빌드는 지시서 10번의 5단계 순서를 10블럭으로 쪼개 그대로 따른다
- 왜: 지시서 11번 "한 번에 전체 스펙 구현 금지" — 코어 루프(1~4블럭)가 돌아야 나머지가 의미 있음
- 버린 대안: 화면 등장 순서대로(온보딩부터) — 인증은 2단계라는 지시서 순서와 충돌
- 영향: plan.md 조립 순서, 홈 달력·제출 완료 화면은 블럭 3에 임시판 → 블럭 7·9에서 완성판

## [2026-07-05] 기획 인터뷰(8단계)를 생략하고 지시서 분해로 대체한다
- 왜: 사용자 지시("지시서에 답 있는 건 다시 묻지 마") + 지시서 0번("이 문서를 구조화하라") — 인터뷰가 물을 내용의 대부분이 지시서에 이미 있음
- 버린 대안: blueprint 8단계 인터뷰 전체 진행 — 이미 답된 것을 되물어 신뢰만 깎임
- 영향: 판단 불가 항목 16건은 09-open-questions.md 로 모아 사용자 결정 대기

## [2026-07-05] 관계는 꼬리표 컬럼만, FK 제약 없음 (킷 규칙 유지)
- 왜: 킷 스택 규칙 — 삭제·구조 변경이 유연하고 "부모 먼저" 류 에러가 없음. 지시서와 충돌 없음
- 버린 대안: FK 제약 — Supabase 중첩 조회는 편하지만 스키마 변경 경직
- 영향: 조인은 두 번 나눠 읽거나 뷰로. 정합성은 soft delete + 앱 로직

## [2026-07-05] 언어는 브라우저 Accept-Language로 자동 판별 (ko 아니면 전부 en, 스위치 없음)
- 왜: 사용자 지시 "영어권은 영어로 자동" — 레딧(영어권) 공개 전 필수. 서버가 요청마다 판별하므로 첫 화면부터 올바른 언어(깜빡임 없음)
- 버린 대안: 언어 선택 버튼(사용자 조작 필요) / IP 위치 판별(부정확·비용) / 별도 URL(/en — 링크 두 벌 관리)
- 영향: messages/en.json 신설, lib/locale.ts·lib/i18n.tsx 배급기, 전 화면 useMessages() 사용, API 오류 문구도 동일 규칙. 헤더 없으면(봇) ko

## [2026-07-05] 횟수 제한은 창고 장애 시 막지 않고 통과시킨다 (fail-open)
- 왜: 제한은 AI 비용 방어 장치지 관문이 아님 — 카운터 표 장애가 서비스 전체를 멈추면 손해가 더 큼
- 버린 대안: 판정 불가 시 차단(fail-closed) — 창고 순단에 무고한 사용자까지 전부 막힘
- 영향: measure_limits 접근은 서버 전용 마스터 열쇠(RLS 공개 정책 없음), 실패한 측정은 횟수를 안 태움, 하루 경계는 서버 날짜(UTC)

## [2026-07-06] 재미의 정의 = 명료화의 카타르시스 (게임 장치 아님)
- 왜: 사용자 확정 — 추상적이던 내가 정확히 비춰져 "아, 맞아"가 나오는 탄식이 이 제품의 재미. 배지 없이도 재미는 기제에서 나온다
- 버린 대안: 게임화 장치(배지·포인트·레벨·축하) — 지시서 11번 금지 + 점수용 기록은 연구 자산 오염
- 영향: 문구·UX 다듬기의 기준 = 거울의 정밀도(인용 정확도·구별의 날카로움·회수 타이밍). CLAUDE.md 관점 원칙에 추가

## [2026-07-06] 과금은 종량제 대신 코스 멤버 혜택(빠른 거울)으로
- 왜: 질 좋은 21일 데이터 목표와 종량제가 충돌(흩어진 낱개 기록) + 소액결제 수수료 + 필요 순간의 결제벽 + 49,000 사다리 잠식
- 버린 대안: 하루 1회 무료 + 추가 건당 결제(충전권 지갑 시스템)
- 영향: PRICING_SPEC 개정, plan.md 4-7 신설, 무료층 변화 없음. 제안서: changes/2026-07-06-빠른거울-코스혜택.md

## [2026-07-06] 익명 신원 = 브라우저 비밀 열쇠(user_id UUID), observer_code 는 표시용 라벨
- 왜: 주소·비번 없는 익명 저장 요구 — 기기에 보관한 랜덤 UUID를 신원으로, 서버가 그 열쇠로만 내 데이터 접근. 데이터 격리(절대원칙 6)는 코드가 아니라 이 열쇠로 잠금
- 버린 대안: observer_code 자체를 신원으로(사칭 위험) / 즉시 이메일 인증(첫 화면까지 멀어짐 — 매직링크는 3-1로)
- 영향: 서버 라우트 service_role 접근, 코드 발급은 max+1(레터 다음=최소 56)+중복시 재발급, 기존 코드 이어받기는 미등록 코드만 허용(이미 등록시 409 — 기기 넘어 되찾기는 3-1). 저장은 trial3 여정+daily_entries

## [2026-07-06] 오제로 아이디 발급 = 무료 3일 미니 거울(trial3) 시작 (PRICING_SPEC대로)
- 왜: 스펙 "가입 → trial3 → daily_entries 저장". ID 발급 시 trial3 journey + 첫 daily_entry(day_no=1)를 함께 만들어 저장의 집을 스펙 위치에 둔다
- 버린 대안: 별도 "맛보기 저장" 테이블 신설 — 스키마 늘리고 스펙과 어긋남
- 영향: 3-0에서 journeys/daily_entries 사용 시작(trial3 UI·게이팅은 3-3/블럭4~5). 연구 동의(3-2) 없이도 본인 저장은 됨 — 연구 추출만 동의로 게이팅

## [2026-07-06] AI 말투는 자세 규정 정본을 시스템 프롬프트로 주입한다 (파인튜닝 아님)
- 왜: 사용자 제공 "AI 자세 규정"이 말투·금지표현·질문 규칙의 정본 — 모델은 매 호출마다 이 수칙을 읽고 따르므로 학습이 필요 없음
- 버린 대안: 파인튜닝(모델 재훈련) — 비용·시간 크고 지시서 11번 v1 금지, 필요도 없음
- 영향: docs/ozero_mirror_ai_자세규정.md 신설, 측정 프롬프트 개정, 블럭 4~5의 코스 AI에 전문 주입 예정. CLAUDE.md에 정본 위치 명시

## [2026-07-06] 열쇠는 빌드 새김(next.config env) 대신 서버 런타임 읽기로
- 왜: env 목록은 빌드 시점 값을 코드에 새겨 넣음 — Vercel 민감 정책으로 빌드 때 열쇠가 안 보이면 빈 값이 새겨져 런타임 주입을 가림 (라이브 저장 불통의 근본 원인이었음)
- 버린 대안: 민감 해제(plain 전환 — 팀 정책·재발 여지) / NEXT_PUBLIC_ 접두사(킷 금지)
- 영향: next.config env 에서 SUPABASE_URL/ANON_KEY 제거, 서버는 런타임에 읽음. 클라이언트가 필요해지는 3-1(로그인)에서 서버 경유 전달로 설계

## [2026-07-07] 무료 측정의 대조를 조각별 나란히 + 카메라 이유로 강화한다
- 왜: 사용자 지적 — 망상을 사실에 놓아도 짚어주지 않으면 거울이 아니라 채점표 없는 시험지. 지시서 6단계("이 문장은 이렇게도 나뉩니다")의 완성
- 버린 대안: 정답 선언("틀렸어요")·일치율 점수 — 지시서 금지(채점·평가). 앱이 내주는 행동 숙제 — 원칙 5(행동의 주어는 사용자) 위반, 행동은 21일 루프에서 사용자가 정함
- 영향: /api/measure 입출력 개편(조각+내 구별 전송 → 조각별 거울 판별+이유), 결과 화면 개편, 한도 입구 사전 안내(GET), 아이디 보유자 1일 3회(스펙 "로그인 3회" 적용)

## [2026-07-07] 무료 측정을 "사실 칸 다시 쓰기 + 거울 검증"으로 교체 + 판별 기준에 구체성 검사 추가
- 왜: 사용자 설계 지시 — 기록 전부 쓰기 → 기준 안내 → 사실만 다시 쓰기 → 오염 짚기 (자세규정 8장의 화면화). 태깅 방식은 기계 조각내기가 한국어 인용 구조를 찢는 문제 + "사실은 구체 이름·숫자"("좋은 호텔"→"리츠칼튼") 기준 확정
- 버린 대안: 조각 태깅 유지 — 다시 쓰기가 훈련 강도·정밀도 모두 우위. 오염 전부 교정 — 훈계가 됨(8-6, 1~2곳만)
- 영향: /api/measure 입출력 개편({text,facts}→성분 검사), 측정 화면 2페이지, 프롬프트에 구체성·발생/내용 분리 규칙, CLAUDE.md 판독 규칙 2줄 추가. 제안서: changes/2026-07-07-사실칸-검증-흐름.md

## [2026-07-07] 가입 장치 = 미완결 질문의 재회 (저장이 아니라 돌아옴을 판다)
- 왜: 사용자 확정 — "아 그렇구나(어쩌라고?)"로 끝나면 가입 이유가 없다. 질문을 깊게(해석 아래의 감정·반복·예측을 여는 형태) + 결과 화면에서 바로 답을 남기게 + "내일 정한 시간에 오늘의 답과 그 거울이 돌아온다"를 가입의 약속으로
- 버린 대안: "저장돼요" 문구(서랍은 가입 이유가 안 됨) / 배지·포인트(지시서 금지)
- 영향: 답변 칸+answer_text 저장, /api/observer/entry(기존 아이디 당일 저장)·/api/observer/recall(어제 질문·답+거울 되비춤, 생성물은 question_quote_text에 보관) 신설, 가입 문구·시간 라벨 개편, o000(운영자 번호) 허용, 모드 전환 시 오류 초기화

## [2026-07-07] 측정을 레터의 세 칸(사실/느낌/행동)으로 확장 + 홈에 이득 두 줄
- 왜: 사용자 확정 — 레터 수료자 55명이 훈련한 검증된 형식. 해석 칸은 불필요(원기록이 해석 원본, 세 칸을 골라내고 남는 것이 망상 = 빼기 정의). 느낌 칸이 "감정 부정 금지"를 구조로 해결(발생의 집). 홈 두 줄 = 타깃(마음공부 오래 한 2040 전문직 여성)에게 위로 아닌 정확함이 신뢰 신호
- 버린 대안: 해석 칸 포함 4분할(두 번 쓰기 중복) / "느낌·감정" 병기(느낌 한 단어 — 몸의 감각 층을 부름) / 느낌 칸 AI 검증(감정 부정이 됨 — 무검증)
- 영향: /api/measure 입력 {text,facts,feelings,actions}, 느낌 무검증·행동 가벼운 검사(action_note), 질문이 느낌 칸을 재료로 사슬(사건→해석→감정)을 물음. 저장은 user_split jsonb에 세 칸. 홈 line1/line2 (영어 병행)

## [2026-07-07] 사흘의 거울을 지금 짓고, 21일 안내는 "곧 열려요" 카드로 (결제는 블럭 10 유지)
- 왜: 가입 문구가 이미 "3일을 남기면 사흘의 반복을 되비춰드려요"를 약속 — 약속은 지어져 있어야 함. 21일 루프(블럭 4·5)가 없는데 결제부터 열면 산 사람이 할 코스가 없음
- 버린 대안: 결제창 먼저 — 스펙 빌드 순서(결제 맨 마지막) 위반 / 대기자 이메일 수집 — 3-1(이메일 연결)과 중복이라 그때 통합
- 영향: /api/observer/mirror3(인용+날짜+빈도만, 원문 검증, 유형명·점수 금지), /me에 사흘의 거울 + 코스 카드(가격은 lib/pricing.ts 설정), 카드 노출 조건 = 3일 이상

## [2026-07-07] 사용자 지시로 빌드 순서 해제 — 블럭 4·5·10 일괄 구축 + 검증 도구 신설
- 왜: 사용자 /goal 명령 — "결제까지 지금 다 완성, 3일·21일 검증 가능하게, 21일 여정 완성까지 멈추지 마" (지시서 10번의 단계별 확인 조항을 사용자가 명시 해제)
- 버린 대안: 단계별 확인 유지 — 발주자가 속도를 택함. USD 실결제(해외 PSP) — 계정 없이는 불가라 테스트 시뮬레이션으로 명시 기록(provider test_usd)
- 영향: 하루 10단계 루프(/today)·중간거울 7/14·21일 보고서·결제(토스 문서 공개 테스트키+USD 테스트)·환불 규칙·빠른거울 10회·검증도구(/seed, 공개 전 제거 대상). 탈중심화 척도는 라이선스 미확인으로 보류, WHO-5만 사용(S16 규칙: 미확인 척도 빌드 금지). payments 표는 block10.sql 사용자 실행 대기

## [2026-07-08] 검증 도구에 "내가 쓴 기록으로 심기" 추가 — AI 가 하루씩 갈라 심는 방식
- 왜: 사용자 요청 — 예문이 아니라 본인이 직접 쓴 3일·21일치 기록으로 반복의 거울·보고서를 검증하고 싶음 (거울의 정밀도는 내 문장으로 봐야 보임)
- 버린 대안: 사실/망상을 사용자가 날마다 직접 표시 — 검증 도구치고 손이 너무 감 / 21일치 한 번에 서버에서 처리 — AI 21회 호출이 서버 시간 제한에 걸림 → 브라우저가 하루씩 호출(진행 표시)
- 영향: /api/dev/seed 에 custom-init(판 깔기)·custom-day(하루 심기, o999 열쇠만 허용) 모드, /seed 에 입력칸+진행 표시. 규칙: 빈 줄 = 하루 구분, "답:" 줄 = 그날 답, 덩어리가 모자라면 기록 없는 날. 감정 이름은 지어내지 않고 비워둠 (절대 규칙 2)

## [2026-07-08] 검증 보고서 후속 — 번호 발급 시험 대역 제외 + /seed 운영자 잠금 + 측정 입구 버튼 잠금
- 왜: Playwright 여정 검증에서 발견 — o999(시험 관찰자)가 번호 최댓값이라 신규 가입자가 o1000대를 받는 라이브 문제 + 등록 관찰자면 누구나 시험 도구 접근 가능 + 한도 차단 대상자에게 쓰기 화면이 먼저 보이는 깜빡임
- 버린 대안: 시험 관찰자 완전 삭제(사용자 검증 도구 상실) / /seed 완전 제거(같은 이유 — 공개 직전에 제거하는 계획은 유지) / 익명 한도의 기기 기준 전환(스티커 지우기로 우회 가능해 IP 유지, 아이디 보유자의 열쇠 기준 분리는 추후 과제)
- 영향: 번호 발급에서 900번대 이상 제외(다음 실가입자 o060 — 실측 확인), /api/dev/seed 는 o000·o999 열쇠만(403 operator-only), 측정 제출 버튼은 한도 확인 후 열림. ⚠ o1000 실기록 1건(07-08 01:27 KST, 시험 데이터 아님)은 보존 — 처분은 사용자 확인 대기

## [2026-07-08] 반복 구조 엔진 E-2·E-3 — 표 없이, 프롬프트+검증기로 (사용자 승인: "둘째 글 방식, 너의 추천대로")
- 왜: 카타르시스의 원천 = 시간축 반복의 명명 (다른 사건, 같은 해석 구조). 사람 라벨·바넘 방지가 생명이라 근거 인용 3중 검증(원문 대조·다른 날짜 2+·반사가 검증 조각 포함, 실패 시 반사 폐기)을 서버에 강제
- 버린 대안: pattern_map 새 표 + 계열 꼬리표(E-1) — 21일 분량은 원문 통째 주입으로 충분, 최소 코드 원칙. 파인튜닝·벡터DB — 기존 금지 유지. "당신은 ~한 사람" 공식(첫 설계문서의 예시) — 둘째 문서·절대 원칙에 따라 구조에만 명명
- 영향: /api/today/question 확장(과거 10→21일, reflection+evidence+보정 질문), /today S09 렌더 추가, 보정 답은 answer_text 재사용으로 다음 반사에 자동 주입. E-4(중간 거울·보고서 확장) 대기

## [2026-07-08] 관리자 대시보드(/admin) — 인증은 운영자 암호(.env ADMIN_KEY), 열람 전용
- 왜: 사용자 요청("기록과 AI 답변 다 볼 수 있게"). A01 명세의 is_admin 이메일 로그인은 3-1(이메일 연결)이 아직 없어 불가 — 암호 방식이 지금 가능한 최소 안전장치 (틀리면 404, 미설정이면 항상 거절)
- 버린 대안: o000 열쇠 기준 인증 — o000 계정의 주인이 불확실(시험 잔재 의혹, 사용자 확인 대기)해서 보류 / 수동 등록(이메일 초대) — 3-1 뒤로
- 영향: /admin(목록·상세·이탈 지표 3종·환불 자리), /api/admin·/api/admin/user (서버 전용 키 열람, 수정·삭제 없음). 라이브는 사용자가 Vercel 에 ADMIN_KEY 추가 후 활성. o1000(01:27)·o060(02:11) 실사용자 확인 — 보존, o060 은 번호 수정이 실트래픽에서 작동한 증거

## [2026-07-08] 저녁 질문 푸시(블럭 8-1) — 웹 푸시 + 매시 노크(GitHub Actions) + 무인증 멱등 발송기
- 왜: 사용자 지시("푸시기능 만들어") — 재회 약속("내일 그 시간에 돌아온다")을 지키는 노크 소리가 없었음. 이메일 채널은 RESEND_API_KEY 가 견본 값이라 사용자 계정 생성 대기
- 버린 대안: Vercel Cron — 무료 요금제는 하루 1회 제한이라 시각별 발송 불가 → GitHub Actions 매시 정각 / 발송기 인증 — 기록창 날짜당 1회 멱등이라 무인증이어도 중복·남용 무해(비밀 관리 1개 감소) / 아이폰 네이티브 앱 — 웹 푸시는 iOS 16.4+ 홈 화면 추가(PWA)로 수신 가능, 명함(manifest) 추가
- 영향: push_subs 표(block8.sql — 사용자 실행 대기, 3-1용 email 칸 포함), sw.js 안테나, /api/push/subscribe·tick, /me 알림 스위치+아이폰 안내, VAPID 열쇠(.env + Vercel 등록 대기), .github/workflows/push-tick.yml. 실기기 수신 검증은 사용자 폰에서 (환경 제약으로 로컬 GUI 불가)

## [2026-07-08] Vercel 열쇠 자동 등록 근본 사고 재발 + 수정 — VAPID_PUBLIC_KEY 도 next.config 빌드 새김에 당함
- 왜: 사용자가 발급한 새 팀 범위 토큰으로 ADMIN_KEY·VAPID 3종을 API로 등록·재배포했으나 VAPID_PUBLIC_KEY만 계속 빈값. health 진단(v6) 실측으로 원인 확정 — next.config env 목록(빌드 시점 새김)에 있는 값만 실패, 목록 밖(런타임 읽기)인 ADMIN_KEY·VAPID_PRIVATE_KEY는 정상. 2026-07-06 SUPABASE_URL/ANON_KEY 사고와 동일 패턴 재확인
- 버린 대안: VAPID_PUBLIC_KEY를 next.config 목록에 그대로 두고 재배포만 반복 — 3차 재배포(2회 API 재배포 + 1회 진짜 git push)까지 전부 실패해 목록 자체가 원인임을 확정 후 폐기
- 영향: VAPID_PUBLIC_KEY를 next.config env 목록에서 제거, /api/push/subscribe(GET)로 런타임 전달하도록 변경(ADMIN_KEY와 같은 안전한 패턴), Vercel 재등록(encrypted 타입). health 진단에 웹 푸시 열쇠 3종 모양 확인 추가(v6). 라이브 실측: 열쇠 7종 전부 "있음", /api/admin 200(관찰자 8명)·404 게이트 정상, /api/push/tick 이 vapid-missing 통과 후 table-missing(=block8.sql 대기)까지 도달

## [2026-07-08] AI 출력 언어 = 화면 언어 (기록 언어 추종 폐기) + 모든 질문 물음표
- 왜: 사용자 지시 — 영어 화면에서 한글 기록을 쓰면 질문이 한글로 나옴. 화면 언어가 곧 사용자가 읽는 언어
- 버린 대안: 기록 언어 추종(기존) — 다국어 기기 사용자에게 어긋남 / 클라이언트 번역 — AI가 처음부터 그 언어로 말하는 게 정확
- 영향: 7개 AI 창구에 langLine(locale) 주입 + ensureQuestionMark 서버 보정(AI가 빼먹어도 ?). 인용 조각만 원문 유지. 영어 경로에서 반사(구조 문장)는 접지 검증에 걸리면 조용히 생략될 수 있음(바넘 방지 우선)

## [2026-07-08] 3-1 이메일 = 자체 서명 토큰(HMAC·30분) + Resend/Gmail 겸용 발송기
- 왜: 실사용자 신원이 브라우저에만 있는 위험. Supabase Auth 매직링크는 시간당 2통 제한 + Site URL 설정 의존이라 부적합
- 버린 대안: Supabase Auth / 비밀번호 계정(익명성 훼손) / 복구 코드 수동 발급(운영 부담)
- 영향: lib/linktoken(위조 불가 도장)·lib/mail(자격증명 자동 선택), /api/email/link·recover, /me 연결 구역, /recover 페이지. 한 이메일=한 아이디, 미등록 이메일에도 같은 응답(열거 방지). 실발송은 Gmail 앱 비밀번호 또는 Resend+도메인 대기 — 그 전까지 운영자 시험 모드로 검증

## [2026-07-08] 반사 접지 검증 공용화 + AI JSON 해석 강건화
- 왜: E-4 검증 중 발견 — 모델이 근거를 문장 전체로 주면 "반사가 근거를 통째로 포함" 검사가 과도하게 엄격, JSON 앞 설명문이 붙으면 해석 전체 실패, 중간거울 지시문이 "사흘"로 굳어 14일을 안 봄
- 버린 대안: 검증 완화(포함 검사 제거) — 바넘 차단력 상실이라 기각
- 영향: reflectionGrounded(따옴표 조각이 검증된 원문 안에 있으면 통과) 3창구 공용, 중괄호 구간만 잘라 읽는 해석기 5창구, 중간거울 기간 중립화("14일 중 5일" 형식)

## [2026-07-08] 보안 리뷰 중간 5건 즉시 반영 (심각 0건 — 리뷰: 요약은 이 문서 없이도 코드 주석에)
- 왜: security-reviewer 전수 점검 — 데이터 유출·파괴는 없으나 정보 노출·비용 남용·토큰 설계 5건이 공개 확장 전 필수
- 버린 대안: 매직링크를 서버 저장 nonce 표로 — 새 표(사용자 SQL) 필요라 기각, 대신 토큰 페이로드 AES-256-GCM 암호화(무DB) / tick 즉시 차단 — 라이브 크론 파괴라 기각, CRON_SECRET-있을때만 강제(비파괴)
- 영향: linktoken.ts(HMAC서명→AES-GCM 암호화, 비밀키 평문 노출 제거) / health(운영자 잠금·404) / email/link(중복 이메일도 sent:true — 열거 차단) / today/suggest(profiles 실존 확인·Opus 남용 차단) / push/tick(CRON_SECRET 게이트) / admin(상수시간 비교) / assess GET(phase 화이트리스트) / UUID 정규식 표준화 18곳. 6~9 하드닝 중 6·7·8 반영, 9(계정 무제한)는 공개 시 캡차로
