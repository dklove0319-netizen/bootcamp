# S17. 무료 측정 — 즉시 측정 흐름 (스펙 1단계 · 비로그인 가능)

> 근거: docs/PRICING_SPEC.md — 기록 1건 → 분석 1회. 비로그인 1일 1회, 로그인 1일 3회. 결과 저장만 가입 필요.
> 용어: 전 화면 "망상"으로 통일 (2026-07-05 사용자 확정 — 이층 가정 폐기, 06-decisions.md).

## 들어오는 길
- 홈(측정 입구) · 공유 카드의 링크 (블럭 6)

## 흐름 (2026-07-07 개정: 사실 칸 다시 쓰기 — 자세규정 8장의 화면화. 제안서: changes/2026-07-07-사실칸-검증-흐름.md)
1. **입구(홈)**: 서브 "무의식 패턴 분석" + 메인 "오제로의 거울"·"Ozero's Mirror" + "지금 비춰보기" (가격·가입 요구 없음)
2. **쓰기 화면 (한 화면 스크롤)**: 쓰기 가이드 + 오늘의 기록 전부 쓰는 칸 → 사실·망상 기준 안내문(카메라·구체성 — "돈이 없어서 못 산다"→"지갑에 0원", "좋은 호텔"→"리츠칼튼") → **사실만 다시 쓰는 칸** → "거울에 비춰보기". AI 힌트는 이 화면에 절대 없음 — 사용자가 먼저 스스로 거른다
3. (구 조각 태깅 화면 폐지 — 기계 조각내기가 한국어 인용 구조를 찢는 문제. 다시 쓰기가 곧 구별 훈련)
4. **결과 (사실 칸 검증 — 자세규정 8장)**:
   - 거울이 사실 칸을 성분 단위로 검사: 각 성분에 [사실/망상/판정유보] + **오염된 곳 중 가장 선명한 1~2곳에만** 카메라 기준 한 줄 이유 (예: "'좋은'은 카메라에 안 담겨요 — 호텔 이름이 실제로 보인 것이에요"). 전부 교정하면 훈계(8-6). "틀렸다" 금지
   - 완전히 깨끗하면 조용히 통과 — "전부 카메라에 담기는 문장이에요" 관찰 한 줄만, 칭찬 금지
   - 거울 측정값 ("거울은 사실 x : 망상 y 로 나눕니다")
   - 질문은 가장 선명한 오염 하나를 조준, 깨끗하면 기록 전체를 향해 ('왜' 시작 금지, 답 후보 금지) · 유형·성격 라벨 금지 · 위로·조언 금지

## 규칙
- AI 프롬프트는 서버에만.
- **저장 (방향 확정, 블럭 3 구현)**: 기록·구별·측정 결과는 **익명 오제로 아이디**(주소·비번 없이 자동 발급, observer_code 부여) 아래 저장한다. 익명 아이디 발급 시 **하루 1회 배달 시간**을 사용자가 정한다. 저장이 익명 아이디와 한 몸인 이유: 신원 없이 저장하면 본인에게 돌려줄(회수) 방법이 없다. 구현 전까지 화면엔 "저장되지 않았어요"를 정직하게 표시.
- 횟수 제한은 서버 판정: 비로그인 1일 1회, **오제로 아이디 보유자 1일 3회** (구현됨 2026-07-07 — 비밀 열쇠가 실제 관찰자일 때만. IP 해시+서버 날짜(UTC), measure_limits, fail-open, 실패한 측정은 안 셈). 초과 시: "오늘 측정은 여기까지예요. 내일 다시 비춰볼 수 있어요." — **입구 진입 시 사전 안내**(GET /api/measure), 구별을 다 시킨 뒤 문 닫지 않기.
- 언어: Accept-Language 자동 판별 — ko 외 전부 en (messages/en.json + 문구 배급기, API 오류 문구 포함. 구현됨 2026-07-05). 영어권 홈은 "Ozero's Mirror"가 메인, 한글 이름이 서브.

## 행동과 흐름
| 행동 | 성공하면 | 실패하면 |
|---|---|---|
| 지금 비춰보기 | 기록 입력 화면 | — |
| 측정 | AI 판독 → 결과 화면 | "측정하지 못했어요. 다시 시도해주세요." (작성 내용 보존) |
| (초과 시) 측정 | — | 위 제한 안내 문구 |

## 예외 5종
| 상황 | 처리 |
|---|---|
| 비어 있을 때 (기록 미작성) | 측정 누르면: "한 줄이라도 적어주세요" |
| 로딩 중 | "비추고 있어요" + 버튼 비활성 (수 초) |
| 실패 (AI·통신) | 문구 + 재시도, 입력 보존. 인용 검증 실패 성분은 버리고 나머지만 표시 |
| 권한 없음 | 해당 없음 (비로그인 허용 화면) |
| 잘못된 입력 | 빈 입력 외 제한 없음 |

## 읽고 쓰는 데이터
- 비로그인: 저장 없음 (일시 처리). 횟수 제한용 최소 식별만 서버에서
- 로그인: 가입자는 측정 결과 저장 (블럭 3에서 정의)

## 만드는 곳
- plan.md 블럭 2 스텝 2-1 ~ 2-4
