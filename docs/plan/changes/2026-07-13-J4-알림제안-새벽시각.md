# 변경 제안서 — J-4 알림 제안 위치 + 새벽 시각 안내 (2026-07-13 · 사용자 승인 "바로 만들어줘")

## ① 뭘 바꾸나 (한 줄)
저녁 알림 켜기를 아이디 발급 완료 화면(가장 뜨거운 순간)에 바로 노출하고, 발급 폼에서 새벽 시각(0~4시)을 고르면 그 시각의 의미를 한 줄로 확인시킨다.

## ② 왜
- 실사용 데이터 (2026-07-13, 가입 35명 기준): 알림 구독 **0명**, 2일 이상 재방문 29% — 돌아올 이유를 배달할 수단이 꺼져 있음. 알림 스위치가 /me 구석에만 있어 발급 직후(전환 최적 순간)에 아무도 못 봄.
- 기록 시각을 새벽 0~2시로 설정한 가입자 6명 — 알림이 켜져도 자정~새벽에 울림. 시각의 의미(기록 창 개방+알림 시각)를 고르는 순간에 알려준 적 없음.

## ③ 영향
- 부품 추출: /me 안에 있던 알림 스위치(NotifySwitch)를 공용 부품으로 분리 — /me 동작 무변경, 발급 완료 화면(SaveMirror done)에 동일 부품 추가.
- 발급 폼: 0~4시 선택 시 안내 한 줄 (선택을 막지 않음 — 야간 근무자 등 실제 새벽 사용자 존중).
- 문구 ko/en. 표·API 무변경. 푸시 정책 무변경 (옵트인 그대로 — 노출 위치만 이동).

## ④ 스텝 계획
- [x] 1. NotifySwitch 공용 부품 분리 + /me 교체 — 확인: /me 알림 구역 기존과 동일 렌더.
- [x] 2. 발급 완료 화면에 알림 스위치 + 폼에 새벽 안내 — 확인: 측정→발급 실주행에서 2시 선택 시 안내 표시·21시 선택 시 숨김, 완료 화면에 알림 켜기 노출.
- [x] 3. plan.md 검증 로그, 커밋·배포.
